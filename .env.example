# Server Configuration
PORT=5000

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/lms-backend-db

# JWT secret key for signing tokens (use a strong, unique secret in production)
JWT_SECRET=@th@rv@

# Optional: JWT expiration time (e.g. 1d, 12h)
JWT_EXPIRES_IN=1d

# Optional: Bcrypt salt rounds
BCRYPT_SALT_ROUNDS=10

# Optional: For sending emails (e.g. for password reset)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=atharva.saawant@example.com
EMAIL_PASS=Atharva@123

# Optional: Cloudinary or AWS credentials for file uploads (if used)
CLOUDINARY_CLOUD_NAME=dfqacxv3m
CLOUDINARY_API_KEY=686436585246257
CLOUDINARY_API_SECRET=MLtrZnwGWio3kZ0w5rvz3UqOMh4
